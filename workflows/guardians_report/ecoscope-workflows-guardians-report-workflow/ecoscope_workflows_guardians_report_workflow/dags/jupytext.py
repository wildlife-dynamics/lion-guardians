# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details


# ruff: noqa: E402

# %% [markdown]
# # Guardians Report
# TODO: top level description

# %% [markdown]
# ## Imports

import os

from ecoscope_workflows_core.tasks.analysis import (
    dataframe_column_max as dataframe_column_max,
)
from ecoscope_workflows_core.tasks.analysis import (
    dataframe_column_mean as dataframe_column_mean,
)
from ecoscope_workflows_core.tasks.analysis import (
    dataframe_column_nunique as dataframe_column_nunique,
)
from ecoscope_workflows_core.tasks.analysis import (
    dataframe_column_sum as dataframe_column_sum,
)
from ecoscope_workflows_core.tasks.config import set_string_var as set_string_var
from ecoscope_workflows_core.tasks.config import (
    set_workflow_details as set_workflow_details,
)
from ecoscope_workflows_core.tasks.filter import (
    get_timezone_from_time_range as get_timezone_from_time_range,
)
from ecoscope_workflows_core.tasks.filter import set_time_range as set_time_range
from ecoscope_workflows_core.tasks.groupby import set_groupers as set_groupers
from ecoscope_workflows_core.tasks.groupby import split_groups as split_groups
from ecoscope_workflows_core.tasks.io import persist_text as persist_text
from ecoscope_workflows_core.tasks.io import set_er_connection as set_er_connection
from ecoscope_workflows_core.tasks.results import (
    create_map_widget_single_view as create_map_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import (
    create_plot_widget_single_view as create_plot_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import (
    create_single_value_widget_single_view as create_single_value_widget_single_view,
)
from ecoscope_workflows_core.tasks.results import gather_dashboard as gather_dashboard
from ecoscope_workflows_core.tasks.results import (
    merge_widget_views as merge_widget_views,
)
from ecoscope_workflows_core.tasks.skip import (
    any_dependency_skipped as any_dependency_skipped,
)
from ecoscope_workflows_core.tasks.skip import any_is_empty_df as any_is_empty_df
from ecoscope_workflows_core.tasks.skip import never as never
from ecoscope_workflows_core.tasks.transformation import (
    add_temporal_index as add_temporal_index,
)
from ecoscope_workflows_core.tasks.transformation import (
    convert_column_values_to_string as convert_column_values_to_string,
)
from ecoscope_workflows_core.tasks.transformation import (
    convert_values_to_timezone as convert_values_to_timezone,
)
from ecoscope_workflows_core.tasks.transformation import map_columns as map_columns
from ecoscope_workflows_core.tasks.transformation import (
    map_values_with_unit as map_values_with_unit,
)
from ecoscope_workflows_core.tasks.transformation import sort_values as sort_values
from ecoscope_workflows_core.tasks.transformation import with_unit as with_unit
from ecoscope_workflows_ext_custom.tasks.io import html_to_png as html_to_png
from ecoscope_workflows_ext_custom.tasks.io import load_df as load_df
from ecoscope_workflows_ext_custom.tasks.results import (
    create_geojson_layer as create_geojson_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_path_layer as create_path_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_scatterplot_layer as create_scatterplot_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import draw_map as draw_map
from ecoscope_workflows_ext_custom.tasks.results import (
    set_base_maps_pydeck as set_base_maps_pydeck,
)
from ecoscope_workflows_ext_custom.tasks.spatial_ops import (
    reproject_gdf as reproject_gdf,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    drop_null_geometry as drop_null_geometry_1,
)
from ecoscope_workflows_ext_ecoscope.tasks.analysis import (
    calculate_linear_time_density as calculate_linear_time_density,
)
from ecoscope_workflows_ext_ecoscope.tasks.analysis import (
    create_meshgrid as create_meshgrid,
)
from ecoscope_workflows_ext_ecoscope.tasks.analysis import summarize_df as summarize_df
from ecoscope_workflows_ext_ecoscope.tasks.io import (
    get_patrols_from_combined_params as get_patrols_from_combined_params,
)
from ecoscope_workflows_ext_ecoscope.tasks.io import persist_df as persist_df
from ecoscope_workflows_ext_ecoscope.tasks.io import (
    set_patrols_and_patrol_events_params as set_patrols_and_patrol_events_params,
)
from ecoscope_workflows_ext_ecoscope.tasks.io import (
    unpack_events_from_patrols_df_and_combined_params as unpack_events_from_patrols_df_and_combined_params,
)
from ecoscope_workflows_ext_ecoscope.tasks.preprocessing import (
    process_relocations as process_relocations,
)
from ecoscope_workflows_ext_ecoscope.tasks.preprocessing import (
    relocations_to_trajectory as relocations_to_trajectory,
)
from ecoscope_workflows_ext_ecoscope.tasks.results import (
    draw_pie_chart as draw_pie_chart,
)
from ecoscope_workflows_ext_ecoscope.tasks.results import (
    draw_time_series_bar_chart as draw_time_series_bar_chart,
)
from ecoscope_workflows_ext_ecoscope.tasks.skip import (
    all_geometry_are_none as all_geometry_are_none,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_color_map as apply_color_map,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_reloc_coord_filter as apply_reloc_coord_filter,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    drop_nan_values_by_column as drop_nan_values_by_column,
)
from ecoscope_workflows_ext_lion_guardians.tasks import (
<<<<<<< HEAD
    create_context_page_lg as create_context_page_lg,
)
from ecoscope_workflows_ext_lion_guardians.tasks import (
    create_guardians_ctx_cover as create_guardians_ctx_cover,
)
from ecoscope_workflows_ext_lion_guardians.tasks import (
    extract_date_parts as extract_date_parts,
)
from ecoscope_workflows_ext_lion_guardians.tasks import (
    generate_guardians_report as generate_guardians_report,
=======
    extract_date_parts as extract_date_parts,
>>>>>>> main
)
from ecoscope_workflows_ext_lion_guardians.tasks import (
    get_event_type_display_names_from_events_aliased as get_event_type_display_names_from_events_aliased,
)
from ecoscope_workflows_ext_lion_guardians.tasks import (
    get_patrol_observations_from_patrols_dataframe_and_combined_params as get_patrol_observations_from_patrols_dataframe_and_combined_params,
)
<<<<<<< HEAD
from ecoscope_workflows_ext_lion_guardians.tasks import guardians_ctx as guardians_ctx
from ecoscope_workflows_ext_lion_guardians.tasks import merge_cl_files as merge_cl_files
=======
>>>>>>> main
from ecoscope_workflows_ext_mnc.tasks import (
    exclude_geom_outliers as exclude_geom_outliers,
)
from ecoscope_workflows_ext_mnc.tasks import pivot_df as pivot_df_1
<<<<<<< HEAD
from ecoscope_workflows_ext_ste.tasks import (
    adjust_map_zoom_and_screenshot as adjust_map_zoom_and_screenshot,
)
=======
>>>>>>> main
from ecoscope_workflows_ext_ste.tasks import (
    combine_deckgl_map_layers as combine_deckgl_map_layers,
)
from ecoscope_workflows_ext_ste.tasks import (
    create_custom_text_layer as create_custom_text_layer,
)
from ecoscope_workflows_ext_ste.tasks import (
    create_deckgl_layer_from_gdf as create_deckgl_layer_from_gdf,
)
from ecoscope_workflows_ext_ste.tasks import (
    custom_view_state_from_gdf as custom_view_state_from_gdf,
)
from ecoscope_workflows_ext_ste.tasks import envelope_gdf as envelope_gdf
from ecoscope_workflows_ext_ste.tasks import (
    fetch_and_persist_file as fetch_and_persist_file,
)
from ecoscope_workflows_ext_ste.tasks import get_gdf_geom_type as get_gdf_geom_type
from ecoscope_workflows_ext_ste.tasks import view_state_deck_gdf as view_state_deck_gdf
from ecoscope_workflows_ext_ste.tasks import zip_groupbykey as zip_groupbykey

# %% [markdown]
# ## Set Workflow Details

# %%
# parameters

workflow_details_params = dict(
    name=...,
    description=...,
    image_url=...,
)

# %%
# call the task


workflow_details = (
    set_workflow_details.set_task_instance_id("workflow_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**workflow_details_params)
    .call()
)


# %% [markdown]
# ## Time range

# %%
# parameters

time_range_params = dict(
    since=...,
    until=...,
    timezone=...,
    time_format=...,
)

# %%
# call the task


time_range = (
    set_time_range.set_task_instance_id("time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_range_params)
    .call()
)


# %% [markdown]
# ## Extract Timezone Selection

# %%
# parameters

get_timezone_params = dict()

# %%
# call the task


get_timezone = (
    get_timezone_from_time_range.set_task_instance_id("get_timezone")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_range=time_range, **get_timezone_params)
    .call()
)


# %% [markdown]
# ## Set groupers

# %%
# parameters

groupers_params = dict(
    groupers=...,
)

# %%
# call the task


groupers = (
    set_groupers.set_task_instance_id("groupers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**groupers_params)
    .call()
)


# %% [markdown]
# ## Connect to ER

# %%
# parameters

er_client_name_params = dict(
    data_source=...,
)

# %%
# call the task


er_client_name = (
    set_er_connection.set_task_instance_id("er_client_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**er_client_name_params)
    .call()
)


# %% [markdown]
# ## Configure base map layers

# %%
# parameters

base_map_defs_params = dict(
    base_maps=...,
)

# %%
# call the task


base_map_defs = (
    set_base_maps_pydeck.set_task_instance_id("base_map_defs")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**base_map_defs_params)
    .call()
)


# %% [markdown]
# ## Download amboseli group ranches

# %%
# parameters

persist_ambo_gpkg_params = dict()

# %%
# call the task


persist_ambo_gpkg = (
    fetch_and_persist_file.set_task_instance_id("persist_ambo_gpkg")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/fcy57d5x5y67gh0xje6rw/lg_group_ranch_boundaries.gpkg?rlkey=yte99xhkte7f1r7n9nemgy7p0&st=sxx1emc1&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_ambo_gpkg_params,
    )
    .call()
)


# %% [markdown]
# ## Download hotspot areas gpkg

# %%
# parameters

persist_hotspot_areas_params = dict()

# %%
# call the task


persist_hotspot_areas = (
    fetch_and_persist_file.set_task_instance_id("persist_hotspot_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/nlozcti0oqhj6bzvsr46g/lg_conflict_hotspots.gpkg?rlkey=giwdizp1j6e24btgh48uxf5v0&st=jivblame&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_hotspot_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Download protected areas gpkg

# %%
# parameters

persist_protected_gpkg_params = dict()

# %%
# call the task


persist_protected_gpkg = (
    fetch_and_persist_file.set_task_instance_id("persist_protected_gpkg")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/i5yczgyln3zh1n8c4ppl5/lg_protected_areas.gpkg?rlkey=5ea21haq2tmsmx7g502p3qag5&st=zt6ztcku&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_protected_gpkg_params,
    )
    .call()
)


# %% [markdown]
# ## Download guardians report cover page

# %%
# parameters

persist_cover_page_params = dict()

# %%
# call the task


persist_cover_page = (
    fetch_and_persist_file.set_task_instance_id("persist_cover_page")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/p1xk9no77w9ctpc4mnn6p/patrol_guardians_cover_page.docx?rlkey=tc5oo54s29wu7cz47glvlbcaj&st=rtwbfa0i&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_cover_page_params,
    )
    .call()
)


# %% [markdown]
# ## Download guardians report  subject page

# %%
# parameters

persist_indv_subject_page_params = dict()

# %%
# call the task


persist_indv_subject_page = (
    fetch_and_persist_file.set_task_instance_id("persist_indv_subject_page")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/kp9mkc9dd5qbast86ufk4/custom_patrol_template.docx?rlkey=ea12bxesuu9dnnj1ngfahhqvr&st=w7q7uf3l&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_indv_subject_page_params,
    )
    .call()
)


# %% [markdown]
# ## Load amboseli group ranches

# %%
# parameters

load_ambo_group_ranches_params = dict()

# %%
# call the task


load_ambo_group_ranches = (
    load_df.set_task_instance_id("load_ambo_group_ranches")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_ambo_gpkg,
        layer=None,
        deserialize_json=False,
        **load_ambo_group_ranches_params,
    )
    .call()
)


# %% [markdown]
# ## Load amboseli group ranches

# %%
# parameters

load_hotspot_areas_params = dict()

# %%
# call the task


load_hotspot_areas = (
    load_df.set_task_instance_id("load_hotspot_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_hotspot_areas,
        layer=None,
        deserialize_json=False,
        **load_hotspot_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Load protected areas

# %%
# parameters

load_protected_areas_params = dict()

# %%
# call the task


load_protected_areas = (
    load_df.set_task_instance_id("load_protected_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_protected_gpkg,
        layer=None,
        deserialize_json=False,
        **load_protected_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for ambo layers

# %%
# parameters

reproject_ambo_boundaries_params = dict()

# %%
# call the task


reproject_ambo_boundaries = (
    reproject_gdf.set_task_instance_id("reproject_ambo_boundaries")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_ambo_group_ranches,
        target_crs="EPSG:4326",
        **reproject_ambo_boundaries_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for hotspot areas

# %%
# parameters

reproject_hotspot_areas_params = dict()

# %%
# call the task


reproject_hotspot_areas = (
    reproject_gdf.set_task_instance_id("reproject_hotspot_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_hotspot_areas, target_crs="EPSG:4326", **reproject_hotspot_areas_params
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for protected areas

# %%
# parameters

reproject_protected_areas_params = dict()

# %%
# call the task


reproject_protected_areas = (
    reproject_gdf.set_task_instance_id("reproject_protected_areas")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=load_protected_areas,
        target_crs="EPSG:4326",
        **reproject_protected_areas_params,
    )
    .call()
)


# %% [markdown]
# ## Annotate amboseli layers with geom type

# %%
# parameters

annotate_ambo_layers_params = dict()

# %%
# call the task


annotate_ambo_layers = (
    get_gdf_geom_type.set_task_instance_id("annotate_ambo_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=reproject_ambo_boundaries, **annotate_ambo_layers_params)
    .call()
)


# %% [markdown]
# ## Annotate hotspot layers with geom type

# %%
# parameters

annotate_hotspot_layers_params = dict()

# %%
# call the task


annotate_hotspot_layers = (
    get_gdf_geom_type.set_task_instance_id("annotate_hotspot_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=reproject_hotspot_areas, **annotate_hotspot_layers_params)
    .call()
)


# %% [markdown]
# ## Annotate protected areas with geom type

# %%
# parameters

annotate_protected_layers_params = dict()

# %%
# call the task


annotate_protected_layers = (
    get_gdf_geom_type.set_task_instance_id("annotate_protected_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=reproject_protected_areas, **annotate_protected_layers_params)
    .call()
)


# %% [markdown]
# ## Create layer for amboseli group ranch

# %%
# parameters

custom_amboseli_layer_params = dict()

# %%
# call the task


custom_amboseli_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("custom_amboseli_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=annotate_ambo_layers,
        style={
            "get_line_color": [169, 169, 169],
            "get_fill_color": [169, 169, 169],
            "get_line_width": 4.5,
            "opacity": 0.55,
            "extruded": False,
            "stroked": True,
            "filled": False,
        },
        legend={
            "title": "Map layers",
            "values": [{"label": "Group ranch boundaries", "color": "#a9a9a9"}],
        },
        **custom_amboseli_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Create layer for hotspot areas

# %%
# parameters

custom_hotspot_layer_params = dict()

# %%
# call the task


custom_hotspot_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("custom_hotspot_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=annotate_hotspot_layers,
        style={
            "get_line_color": [220, 20, 60],
            "get_fill_color": [220, 20, 60],
            "get_radius": 2.55,
            "get_line_width": 1.95,
            "opacity": 0.75,
            "extruded": False,
            "stroked": True,
            "filled": True,
        },
        legend={
            "title": "",
            "values": [{"label": "Hotspot areas", "color": "#dc143c"}],
        },
        **custom_hotspot_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Create layer for protected areas

# %%
# parameters

custom_protected_layer_params = dict()

# %%
# call the task


custom_protected_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("custom_protected_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=annotate_protected_layers,
        style={
            "get_line_color": [77, 102, 0],
            "get_fill_color": [77, 102, 0],
            "get_line_width": 1.95,
            "opacity": 0.35,
            "extruded": False,
            "stroked": True,
            "filled": True,
        },
        legend={
            "title": "",
            "values": [{"label": "National parks and reserves", "color": "#4d6600"}],
        },
        **custom_protected_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Create text layer for conflict hotspots

# %%
# parameters

create_hotspot_text_layer_params = dict()

# %%
# call the task


create_hotspot_text_layer = (
    create_custom_text_layer.set_task_instance_id("create_hotspot_text_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        geodataframe=reproject_hotspot_areas,
        layer_style={
            "get_text": "name",
            "get_color": [20, 20, 20, 255],
            "get_size": 1000,
            "size_units": "meters",
            "size_min_pixels": 40,
            "size_max_pixels": 75,
            "size_scale": 1.25,
            "font_family": "Arial",
            "font_weight": "normal",
            "get_text_anchor": "middle",
            "get_alignment_baseline": "center",
            "billboard": True,
            "background_padding": [4, 8],
            "pickable": True,
            "auto_highlight": False,
        },
        use_centroid=True,
        legend=None,
        **create_hotspot_text_layer_params,
    )
    .call()
)


# %% [markdown]
# ## Set patrol and patrol events params

# %%
# parameters

er_patrol_and_events_params_params = dict(
    patrol_types=...,
    event_types=...,
    status=...,
    include_null_geometry=...,
)

# %%
# call the task


er_patrol_and_events_params = (
    set_patrols_and_patrol_events_params.set_task_instance_id(
        "er_patrol_and_events_params"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=time_range,
        include_patrol_details=True,
        raise_on_empty=False,
        truncate_to_time_range=True,
        sub_page_size=150,
        **er_patrol_and_events_params_params,
    )
    .call()
)


# %% [markdown]
# ## Prefetch patrols

# %%
# parameters

prefetch_patrols_params = dict()

# %%
# call the task


prefetch_patrols = (
    get_patrols_from_combined_params.set_task_instance_id("prefetch_patrols")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(combined_params=er_patrol_and_events_params, **prefetch_patrols_params)
    .call()
)


# %% [markdown]
# ## Get patrol observations

# %%
# parameters

patrol_obs_params = dict()

# %%
# call the task


patrol_obs = (
    get_patrol_observations_from_patrols_dataframe_and_combined_params.set_task_instance_id(
        "patrol_obs"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        patrols_df=prefetch_patrols,
        combined_params=er_patrol_and_events_params,
        **patrol_obs_params,
    )
    .call()
)


# %% [markdown]
# ## Get patrol events

# %%
# parameters

patrol_events_params = dict()

# %%
# call the task


patrol_events = (
    unpack_events_from_patrols_df_and_combined_params.set_task_instance_id(
        "patrol_events"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        patrols_df=prefetch_patrols,
        combined_params=er_patrol_and_events_params,
        **patrol_events_params,
    )
    .call()
)


# %% [markdown]
# ## Get event type display names from events

# %%
# parameters

event_type_display_names_params = dict()

# %%
# call the task


event_type_display_names = (
    get_event_type_display_names_from_events_aliased.set_task_instance_id(
        "event_type_display_names"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        events_gdf=patrol_events,
        append_category_names="duplicates",
        **event_type_display_names_params,
    )
    .call()
)


# %% [markdown]
# ## Convert patrols to timezone

# %%
# parameters

convert_patrols_to_user_timezone_params = dict()

# %%
# call the task


convert_patrols_to_user_timezone = (
    convert_values_to_timezone.set_task_instance_id("convert_patrols_to_user_timezone")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=patrol_obs,
        timezone=get_timezone,
        columns=["patrol_start_time", "patrol_end_time", "fixtime"],
        **convert_patrols_to_user_timezone_params,
    )
    .call()
)


# %% [markdown]
# ## Convert events to timezone

# %%
# parameters

convert_events_to_user_timezone_params = dict()

# %%
# call the task


convert_events_to_user_timezone = (
    convert_values_to_timezone.set_task_instance_id("convert_events_to_user_timezone")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=event_type_display_names,
        timezone=get_timezone,
        columns=["time", "patrol_start_time"],
        **convert_events_to_user_timezone_params,
    )
    .call()
)


# %% [markdown]
# ## Persist events as geoparquet

# %%
# parameters

persist_events_geoparquet_params = dict()

# %%
# call the task


persist_events_geoparquet = (
    persist_df.set_task_instance_id("persist_events_geoparquet")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="geoparquet",
        df=convert_events_to_user_timezone,
        filename="events",
        **persist_events_geoparquet_params,
    )
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

set_patrol_traj_color_column_params = dict(
    var=...,
)

# %%
# call the task


set_patrol_traj_color_column = (
    set_string_var.set_task_instance_id("set_patrol_traj_color_column")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**set_patrol_traj_color_column_params)
    .call()
)


# %% [markdown]
# ## Transform Observations to Relocations

# %%
# parameters

patrol_reloc_params = dict()

# %%
# call the task


patrol_reloc = (
    process_relocations.set_task_instance_id("patrol_reloc")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        observations=convert_patrols_to_user_timezone,
        relocs_columns=[
            "patrol_id",
            "patrol_start_time",
            "patrol_end_time",
            "patrol_type__value",
            "patrol_type__display",
            "patrol_serial_number",
            "patrol_status",
            "patrol_subject",
            "groupby_col",
            "fixtime",
            "junk_status",
            "extra__source",
            "geometry",
        ],
        filter_point_coords=[
            {"x": 180.0, "y": 90.0},
            {"x": 0.0, "y": 0.0},
            {"x": 1.0, "y": 1.0},
        ],
        **patrol_reloc_params,
    )
    .call()
)


# %% [markdown]
# ## Trajectory segment filter

# %%
# parameters

patrol_traj_params = dict(
    trajectory_segment_filter=...,
)

# %%
# call the task


patrol_traj = (
    relocations_to_trajectory.set_task_instance_id("patrol_traj")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(relocations=patrol_reloc, **patrol_traj_params)
    .call()
)


# %% [markdown]
# ## Add temporal index to patrol trajectories

# %%
# parameters

traj_add_temporal_index_params = dict()

# %%
# call the task


traj_add_temporal_index = (
    add_temporal_index.set_task_instance_id("traj_add_temporal_index")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=patrol_traj,
        time_col="extra__patrol_start_time",
        groupers=groupers,
        cast_to_datetime=True,
        format="mixed",
        **traj_add_temporal_index_params,
    )
    .call()
)


# %% [markdown]
# ## Rename value grouper columns for Trajectories

# %%
# parameters

traj_rename_grouper_columns_params = dict()

# %%
# call the task


traj_rename_grouper_columns = (
    map_columns.set_task_instance_id("traj_rename_grouper_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        raise_if_not_found=True,
        df=traj_add_temporal_index,
        drop_columns=[],
        retain_columns=[],
        rename_columns={
            "extra__patrol_type__value": "patrol_type",
            "extra__patrol_serial_number": "patrol_serial_number",
            "extra__patrol_status": "patrol_status",
            "extra__patrol_subject": "patrol_subject",
        },
        **traj_rename_grouper_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Persist trajectories as geoparquet

# %%
# parameters

persist_patrols_geoparquet_params = dict()

# %%
# call the task


persist_patrols_geoparquet = (
    persist_df.set_task_instance_id("persist_patrols_geoparquet")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="geoparquet",
        df=traj_rename_grouper_columns,
        filename="trajectories",
        **persist_patrols_geoparquet_params,
    )
    .call()
)


# %% [markdown]
# ## Patrol Traj Colormap

# %%
# parameters

traj_colormap_params = dict()

# %%
# call the task


traj_colormap = (
    apply_color_map.set_task_instance_id("traj_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=traj_rename_grouper_columns,
        colormap="Paired",
        input_column_name=set_patrol_traj_color_column,
        output_column_name="patrol_traj_colormap",
        **traj_colormap_params,
    )
    .call()
)


# %% [markdown]
# ## Filter patrol events

# %%
# parameters

filter_patrol_events_params = dict(
    bounding_box=...,
    filter_point_coords=...,
    reset_index=...,
)

# %%
# call the task


filter_patrol_events = (
    apply_reloc_coord_filter.set_task_instance_id("filter_patrol_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_events_to_user_timezone,
        roi_gdf=None,
        roi_name=None,
        **filter_patrol_events_params,
    )
    .call()
)


# %% [markdown]
# ## Add temporal index to patrol events

# %%
# parameters

pe_add_temporal_index_params = dict()

# %%
# call the task


pe_add_temporal_index = (
    add_temporal_index.set_task_instance_id("pe_add_temporal_index")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=filter_patrol_events,
        time_col="patrol_start_time",
        groupers=groupers,
        cast_to_datetime=True,
        format="mixed",
        **pe_add_temporal_index_params,
    )
    .call()
)


# %% [markdown]
# ## Patrol Events Colormap

# %%
# parameters

pe_colormap_params = dict()

# %%
# call the task


pe_colormap = (
    apply_color_map.set_task_instance_id("pe_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=pe_add_temporal_index,
        input_column_name="event_type",
        colormap="Accent",
        output_column_name="event_type_colormap",
        **pe_colormap_params,
    )
    .call()
)


# %% [markdown]
# ## Cast Patrol Trajectory Columns

# %%
# parameters

patrol_traj_cols_to_string_params = dict()

# %%
# call the task


patrol_traj_cols_to_string = (
    convert_column_values_to_string.set_task_instance_id("patrol_traj_cols_to_string")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=traj_colormap,
        columns=["patrol_serial_number", "patrol_type"],
        **patrol_traj_cols_to_string_params,
    )
    .call()
)


# %% [markdown]
# ## Cast Patrol Events Columns

# %%
# parameters

pe_cols_to_string_params = dict()

# %%
# call the task


pe_cols_to_string = (
    convert_column_values_to_string.set_task_instance_id("pe_cols_to_string")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=pe_colormap,
        columns=["patrol_serial_number", "patrol_type"],
        **pe_cols_to_string_params,
    )
    .call()
)


# %% [markdown]
# ## Set Event Map Title

# %%
# parameters

set_event_map_title_params = dict()

# %%
# call the task


set_event_map_title = (
    set_string_var.set_task_instance_id("set_event_map_title")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(var="Patrol Events Map", **set_event_map_title_params)
    .call()
)


# %% [markdown]
# ## Set Combined Trajectory & Event Map Title

# %%
# parameters

set_traj_map_title_params = dict()

# %%
# call the task


set_traj_map_title = (
    set_string_var.set_task_instance_id("set_traj_map_title")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(var="Trajectories Map", **set_traj_map_title_params)
    .call()
)


# %% [markdown]
# ## Set Time Density Map Title

# %%
# parameters

set_ltd_map_title_params = dict()

# %%
# call the task


set_ltd_map_title = (
    set_string_var.set_task_instance_id("set_ltd_map_title")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(var="Time Density Map", **set_ltd_map_title_params)
    .call()
)


# %% [markdown]
# ## Set Bar Chart Title

# %%
# parameters

set_bar_chart_title_params = dict()

# %%
# call the task


set_bar_chart_title = (
    set_string_var.set_task_instance_id("set_bar_chart_title")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(var="Patrol Events Bar Chart", **set_bar_chart_title_params)
    .call()
)


# %% [markdown]
# ## Set Pie Chart Title

# %%
# parameters

set_pie_chart_title_params = dict()

# %%
# call the task


set_pie_chart_title = (
    set_string_var.set_task_instance_id("set_pie_chart_title")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(var="Patrol Events Pie Chart", **set_pie_chart_title_params)
    .call()
)


# %% [markdown]
# ## Split Patrol Trajectories by Group

# %%
# parameters

split_patrol_traj_groups_params = dict()

# %%
# call the task


split_patrol_traj_groups = (
    split_groups.set_task_instance_id("split_patrol_traj_groups")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=patrol_traj_cols_to_string,
        groupers=groupers,
        **split_patrol_traj_groups_params,
    )
    .call()
)


# %% [markdown]
# ## Split Patrol Events by Group

# %%
# parameters

split_pe_groups_params = dict()

# %%
# call the task


split_pe_groups = (
    split_groups.set_task_instance_id("split_pe_groups")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=pe_cols_to_string, groupers=groupers, **split_pe_groups_params)
    .call()
)


# %% [markdown]
# ## Rename patrol events columns for map tooltip display

# %%
# parameters

pe_rename_display_columns_params = dict()

# %%
# call the task


pe_rename_display_columns = (
    map_columns.set_task_instance_id("pe_rename_display_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        raise_if_not_found=True,
        drop_columns=[],
        retain_columns=[],
        rename_columns={
            "patrol_serial_number": "Patrol Serial",
            "serial_number": "Event Serial",
            "event_type_display": "Event Type",
            "time": "Event Time",
        },
        **pe_rename_display_columns_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_pe_groups)
)


# %% [markdown]
# ## Exclude geom outliers from events

# %%
# parameters

exclude_event_outliers_params = dict()

# %%
# call the task


exclude_event_outliers = (
    exclude_geom_outliers.set_task_instance_id("exclude_event_outliers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(z_threshold=3, **exclude_event_outliers_params)
    .mapvalues(argnames=["df"], argvalues=pe_rename_display_columns)
)


# %% [markdown]
# ## Remove event invalid points

# %%
# parameters

remove_invalid_geoms_params = dict()

# %%
# call the task


remove_invalid_geoms = (
    drop_null_geometry_1.set_task_instance_id("remove_invalid_geoms")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(geometry_column="geometry", **remove_invalid_geoms_params)
    .mapvalues(argnames=["gdf"], argvalues=exclude_event_outliers)
)


# %% [markdown]
# ## Create map layers for each Patrols Events group

# %%
# parameters

patrol_events_map_layers_params = dict()

# %%
# call the task


patrol_events_map_layers = (
    create_scatterplot_layer.set_task_instance_id("patrol_events_map_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
            all_geometry_are_none,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_fill_color": "event_type_colormap",
            "get_radius": 5,
            "opacity": 0.75,
            "stroked": True,
        },
        legend={
            "title": "Events recorded",
            "label_column": "Event Type",
            "color_column": "event_type_colormap",
            "sort": "ascending",
            "label_suffix": None,
        },
        **patrol_events_map_layers_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=remove_invalid_geoms)
)


# %% [markdown]
# ## Combine styled layers with events

# %%
# parameters

merge_static_wevent_layers_params = dict()

# %%
# call the task


merge_static_wevent_layers = (
    combine_deckgl_map_layers.set_task_instance_id("merge_static_wevent_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[
            custom_amboseli_layer,
            custom_hotspot_layer,
            custom_protected_layer,
            create_hotspot_text_layer,
        ],
        **merge_static_wevent_layers_params,
    )
    .mapvalues(argnames=["grouped_layers"], argvalues=patrol_events_map_layers)
)


# %% [markdown]
# ## zoom to envelope events

# %%
# parameters

envelope_events_params = dict()

# %%
# call the task


envelope_events = (
    envelope_gdf.set_task_instance_id("envelope_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(expansion_factor=1.05, **envelope_events_params)
    .mapvalues(argnames=["gdf"], argvalues=pe_rename_display_columns)
)


# %% [markdown]
# ## Get image gdf zoom extent

# %%
# parameters

gdf_events_image_extent_params = dict()

# %%
# call the task


gdf_events_image_extent = (
    view_state_deck_gdf.set_task_instance_id("gdf_events_image_extent")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(pitch=0, bearing=0, **gdf_events_image_extent_params)
    .mapvalues(argnames=["gdf"], argvalues=pe_rename_display_columns)
)


# %% [markdown]
# ## Zoom to gdf extent

# %%
# parameters

zoom_events_gdf_extent_params = dict()

# %%
# call the task


zoom_events_gdf_extent = (
    custom_view_state_from_gdf.set_task_instance_id("zoom_events_gdf_extent")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(max_zoom=20, **zoom_events_gdf_extent_params)
    .mapvalues(argnames=["gdf"], argvalues=envelope_events)
)


# %% [markdown]
# ## Combine event layers with view state

# %%
# parameters

zip_events_with_viewstate_params = dict()

# %%
# call the task


zip_events_with_viewstate = (
    zip_groupbykey.set_task_instance_id("zip_events_with_viewstate")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merge_static_wevent_layers, zoom_events_gdf_extent],
        **zip_events_with_viewstate_params,
    )
    .call()
)


# %% [markdown]
# ## Draw events map

# %%
# parameters

draw_events_params = dict()

# %%
# call the task


draw_events = (
    draw_map.set_task_instance_id("draw_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=base_map_defs,
        static=False,
        title=None,
        max_zoom=10,
        widget_id=set_event_map_title,
        legend_style={"placement": "bottom-right"},
        **draw_events_params,
    )
    .mapvalues(
        argnames=["geo_layers", "view_state"], argvalues=zip_events_with_viewstate
    )
)


# %% [markdown]
# ## Persist events html

# %%
# parameters

persist_events_html_params = dict(
    filename=...,
)

# %%
# call the task


persist_events_html = (
    persist_text.set_task_instance_id("persist_events_html")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="events",
        **persist_events_html_params,
    )
    .mapvalues(argnames=["text"], argvalues=draw_events)
<<<<<<< HEAD
)


# %% [markdown]
# ## Combine events path with zoom value

# %%
# parameters

zip_events_value_params = dict()

# %%
# call the task


zip_events_value = (
    zip_groupbykey.set_task_instance_id("zip_events_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[gdf_events_image_extent, persist_events_html],
        **zip_events_value_params,
    )
    .call()
)


# %% [markdown]
# ## Convert events html to png

# %%
# parameters

generate_events_png_params = dict()

# %%
# call the task


generate_events_png = (
    adjust_map_zoom_and_screenshot.set_task_instance_id("generate_events_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        screenshot_config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **generate_events_png_params,
    )
    .mapvalues(argnames=["view_state", "input_file"], argvalues=zip_events_value)
=======
>>>>>>> main
)


# %% [markdown]
# ## Format speed values for display

# %%
# parameters

speed_val_with_unit_params = dict()

# %%
# call the task


speed_val_with_unit = (
    map_values_with_unit.set_task_instance_id("speed_val_with_unit")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        input_column_name="speed_kmhr",
        output_column_name="speed_kmhr",
        original_unit="km/h",
        new_unit="km/h",
        decimal_places=1,
        **speed_val_with_unit_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Rename patrol traj columns for map tooltip display

# %%
# parameters

patrol_traj_rename_columns_params = dict()

# %%
# call the task


patrol_traj_rename_columns = (
    map_columns.set_task_instance_id("patrol_traj_rename_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        raise_if_not_found=True,
        drop_columns=[],
        retain_columns=[
            "speed_kmhr",
            "geometry",
            "timespan_seconds",
            "segment_start",
            "extra__patrol_type__display",
            "patrol_serial_number",
            "patrol_traj_colormap",
            "patrol_type",
            "patrol_status",
            "patrol_subject",
        ],
        rename_columns={
            "patrol_serial_number": "Patrol Serial",
            "extra__patrol_type__display": "Patrol Type",
            "segment_start": "Start",
            "timespan_seconds": "Duration (s)",
            "speed_kmhr": "Speed (kph)",
        },
        **patrol_traj_rename_columns_params,
    )
    .mapvalues(argnames=["df"], argvalues=speed_val_with_unit)
)


# %% [markdown]
# ## Create map layer for each Patrol Trajectories group

# %%
# parameters

patrol_traj_map_layers_params = dict()

# %%
# call the task


patrol_traj_map_layers = (
    create_path_layer.set_task_instance_id("patrol_traj_map_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
            all_geometry_are_none,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_color": "patrol_traj_colormap",
            "get_width": 1.85,
            "width_scale": 1,
            "width_min_pixels": 2,
            "width_max_pixels": 6,
            "width_units": "pixels",
            "cap_rounded": True,
            "joint_rounded": True,
            "billboard": False,
            "opacity": 0.55,
            "stroked": True,
        },
        legend={
            "title": "Patrol Trajectories",
            "label_column": set_patrol_traj_color_column,
            "color_column": "patrol_traj_colormap",
            "sort": "ascending",
            "label_suffix": None,
        },
        **patrol_traj_map_layers_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=patrol_traj_rename_columns)
)


# %% [markdown]
# ## Combine styled layers with trajectories

# %%
# parameters

merge_static_traj_layers_params = dict()

# %%
# call the task


merge_static_traj_layers = (
    combine_deckgl_map_layers.set_task_instance_id("merge_static_traj_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[
            custom_amboseli_layer,
            custom_hotspot_layer,
            custom_protected_layer,
            create_hotspot_text_layer,
        ],
        **merge_static_traj_layers_params,
    )
    .mapvalues(argnames=["grouped_layers"], argvalues=patrol_traj_map_layers)
)


# %% [markdown]
# ## zoom to envelope trajs

# %%
# parameters

envelope_trajs_params = dict()

# %%
# call the task


envelope_trajs = (
    envelope_gdf.set_task_instance_id("envelope_trajs")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(expansion_factor=1.05, **envelope_trajs_params)
    .mapvalues(argnames=["gdf"], argvalues=patrol_traj_rename_columns)
)


# %% [markdown]
# ## Get image gdf trajs zoom extent

# %%
# parameters

gdf_trajs_image_extent_params = dict()

# %%
# call the task


gdf_trajs_image_extent = (
    view_state_deck_gdf.set_task_instance_id("gdf_trajs_image_extent")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(pitch=0, bearing=0, **gdf_trajs_image_extent_params)
    .mapvalues(argnames=["gdf"], argvalues=pe_rename_display_columns)
)


# %% [markdown]
# ## Zoom to gdf extent

# %%
# parameters

zoom_trajs_gdf_extent_params = dict()

# %%
# call the task


zoom_trajs_gdf_extent = (
    custom_view_state_from_gdf.set_task_instance_id("zoom_trajs_gdf_extent")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(max_zoom=20, **zoom_trajs_gdf_extent_params)
    .mapvalues(argnames=["gdf"], argvalues=envelope_trajs)
)


# %% [markdown]
# ## Combine trajectory layers with view state

# %%
# parameters

zip_trajs_with_viewstate_params = dict()

# %%
# call the task


zip_trajs_with_viewstate = (
    zip_groupbykey.set_task_instance_id("zip_trajs_with_viewstate")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merge_static_traj_layers, zoom_trajs_gdf_extent],
        **zip_trajs_with_viewstate_params,
    )
    .call()
)


# %% [markdown]
# ## Draw Ecomaps for each combined Trajectory and Patrol Events group

# %%
# parameters

trajs_ecomap_params = dict()

# %%
# call the task


trajs_ecomap = (
    draw_map.set_task_instance_id("trajs_ecomap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=base_map_defs,
        legend_style={"placement": "bottom-right"},
        static=False,
        title=None,
        max_zoom=10,
        widget_id=set_traj_map_title,
        **trajs_ecomap_params,
    )
    .mapvalues(
        argnames=["geo_layers", "view_state"], argvalues=zip_trajs_with_viewstate
    )
)


# %% [markdown]
# ## Persist patrol trajectories as text

# %%
# parameters

traj_ecomap_html_urls_params = dict(
    filename=...,
)

# %%
# call the task


traj_ecomap_html_urls = (
    persist_text.set_task_instance_id("traj_ecomap_html_urls")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="patrol_trajectories",
        **traj_ecomap_html_urls_params,
    )
    .mapvalues(argnames=["text"], argvalues=trajs_ecomap)
)


# %% [markdown]
<<<<<<< HEAD
# ## Combine trajectories path with zoom value
=======
# ## Create Map Widgets for patrol events
>>>>>>> main

# %%
# parameters

<<<<<<< HEAD
zip_trajs_value_params = dict()
=======
events_map_widgets_single_views_params = dict()
>>>>>>> main

# %%
# call the task


<<<<<<< HEAD
zip_trajs_value = (
    zip_groupbykey.set_task_instance_id("zip_trajs_value")
=======
events_map_widgets_single_views = (
    create_map_widget_single_view.set_task_instance_id(
        "events_map_widgets_single_views"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title=set_event_map_title, **events_map_widgets_single_views_params)
    .map(argnames=["view", "data"], argvalues=persist_events_html)
)


# %% [markdown]
# ## Merge events ecomap widgets

# %%
# parameters

events_grouped_map_widget_params = dict()

# %%
# call the task


events_grouped_map_widget = (
    merge_widget_views.set_task_instance_id("events_grouped_map_widget")
>>>>>>> main
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
<<<<<<< HEAD
        sequences=[gdf_trajs_image_extent, traj_ecomap_html_urls],
        **zip_trajs_value_params,
=======
        widgets=events_map_widgets_single_views, **events_grouped_map_widget_params
>>>>>>> main
    )
    .call()
)


# %% [markdown]
<<<<<<< HEAD
# ## Convert trajs html to png
=======
# ## Create Map Widgets for patrol trajectories
>>>>>>> main

# %%
# parameters

<<<<<<< HEAD
generate_trajs_png_params = dict()
=======
trajs_map_widgets_single_views_params = dict()
>>>>>>> main

# %%
# call the task


<<<<<<< HEAD
generate_trajs_png = (
    adjust_map_zoom_and_screenshot.set_task_instance_id("generate_trajs_png")
=======
trajs_map_widgets_single_views = (
    create_map_widget_single_view.set_task_instance_id("trajs_map_widgets_single_views")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title=set_traj_map_title, **trajs_map_widgets_single_views_params)
    .map(argnames=["view", "data"], argvalues=traj_ecomap_html_urls)
)


# %% [markdown]
# ## Merge trajectories ecomap widgets

# %%
# parameters

trajs_grouped_map_widget_params = dict()

# %%
# call the task


trajs_grouped_map_widget = (
    merge_widget_views.set_task_instance_id("trajs_grouped_map_widget")
>>>>>>> main
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
<<<<<<< HEAD
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        screenshot_config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **generate_trajs_png_params,
    )
    .mapvalues(argnames=["view_state", "input_file"], argvalues=zip_trajs_value)
=======
    .partial(widgets=trajs_map_widgets_single_views, **trajs_grouped_map_widget_params)
    .call()
>>>>>>> main
)


# %% [markdown]
<<<<<<< HEAD
# ## Create Map Widgets for patrol events

# %%
# parameters

events_map_widgets_single_views_params = dict()

# %%
# call the task


events_map_widgets_single_views = (
    create_map_widget_single_view.set_task_instance_id(
        "events_map_widgets_single_views"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title=set_event_map_title, **events_map_widgets_single_views_params)
    .map(argnames=["view", "data"], argvalues=persist_events_html)
)


# %% [markdown]
# ## Merge events ecomap widgets

# %%
# parameters

events_grouped_map_widget_params = dict()

# %%
# call the task


events_grouped_map_widget = (
    merge_widget_views.set_task_instance_id("events_grouped_map_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        widgets=events_map_widgets_single_views, **events_grouped_map_widget_params
    )
    .call()
)


# %% [markdown]
# ## Create Map Widgets for patrol trajectories

# %%
# parameters

trajs_map_widgets_single_views_params = dict()

# %%
# call the task


trajs_map_widgets_single_views = (
    create_map_widget_single_view.set_task_instance_id("trajs_map_widgets_single_views")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title=set_traj_map_title, **trajs_map_widgets_single_views_params)
    .map(argnames=["view", "data"], argvalues=traj_ecomap_html_urls)
)


# %% [markdown]
# ## Merge trajectories ecomap widgets

# %%
# parameters

trajs_grouped_map_widget_params = dict()

# %%
# call the task


trajs_grouped_map_widget = (
    merge_widget_views.set_task_instance_id("trajs_grouped_map_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=trajs_map_widgets_single_views, **trajs_grouped_map_widget_params)
    .call()
)


# %% [markdown]
=======
>>>>>>> main
# ## Patrol summary statistics

# %%
# parameters

total_patrols_params = dict()

# %%
# call the task


total_patrols = (
    dataframe_column_nunique.set_task_instance_id("total_patrols")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="extra__patrol_id", **total_patrols_params)
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Create Single Value Widgets for Total Patrols Per Group

# %%
# parameters

total_patrols_sv_widgets_params = dict()

# %%
# call the task


total_patrols_sv_widgets = (
    create_single_value_widget_single_view.set_task_instance_id(
        "total_patrols_sv_widgets"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Total Patrols", decimal_places=1, **total_patrols_sv_widgets_params)
    .map(argnames=["view", "data"], argvalues=total_patrols)
)


# %% [markdown]
# ## Merge per group Total Patrols SV widgets

# %%
# parameters

total_patrols_grouped_sv_widget_params = dict()

# %%
# call the task


total_patrols_grouped_sv_widget = (
    merge_widget_views.set_task_instance_id("total_patrols_grouped_sv_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=total_patrols_sv_widgets, **total_patrols_grouped_sv_widget_params)
    .call()
)


# %% [markdown]
# ## Calculate Total Patrol Time Per Group

# %%
# parameters

total_patrol_time_params = dict()

# %%
# call the task


total_patrol_time = (
    dataframe_column_sum.set_task_instance_id("total_patrol_time")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="timespan_seconds", **total_patrol_time_params)
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Convert total patrol time units

# %%
# parameters

total_patrol_time_converted_params = dict()

# %%
# call the task


total_patrol_time_converted = (
    with_unit.set_task_instance_id("total_patrol_time_converted")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(original_unit="s", new_unit="h", **total_patrol_time_converted_params)
    .mapvalues(argnames=["value"], argvalues=total_patrol_time)
)


# %% [markdown]
# ## Create Single Value Widgets for Total Patrol Time Per Group

# %%
# parameters

total_patrol_time_sv_widgets_params = dict()

# %%
# call the task


total_patrol_time_sv_widgets = (
    create_single_value_widget_single_view.set_task_instance_id(
        "total_patrol_time_sv_widgets"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(
        title="Total Time", decimal_places=1, **total_patrol_time_sv_widgets_params
    )
    .map(argnames=["view", "data"], argvalues=total_patrol_time_converted)
)


# %% [markdown]
# ## Merge per group Total Patrol Time SV widgets

# %%
# parameters

patrol_time_grouped_widget_params = dict()

# %%
# call the task


patrol_time_grouped_widget = (
    merge_widget_views.set_task_instance_id("patrol_time_grouped_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=total_patrol_time_sv_widgets, **patrol_time_grouped_widget_params)
    .call()
)


# %% [markdown]
# ## Calculate Total Distance Per Group

# %%
# parameters

total_patrol_dist_params = dict()

# %%
# call the task


total_patrol_dist = (
    dataframe_column_sum.set_task_instance_id("total_patrol_dist")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="dist_meters", **total_patrol_dist_params)
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Convert total patrol distance units

# %%
# parameters

total_patrol_dist_converted_params = dict()

# %%
# call the task


total_patrol_dist_converted = (
    with_unit.set_task_instance_id("total_patrol_dist_converted")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(original_unit="m", new_unit="km", **total_patrol_dist_converted_params)
    .mapvalues(argnames=["value"], argvalues=total_patrol_dist)
)


# %% [markdown]
# ## Create Single Value Widgets for Total Distance Per Group

# %%
# parameters

total_patrol_dist_sv_widgets_params = dict()

# %%
# call the task


total_patrol_dist_sv_widgets = (
    create_single_value_widget_single_view.set_task_instance_id(
        "total_patrol_dist_sv_widgets"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(
        title="Total Distance", decimal_places=1, **total_patrol_dist_sv_widgets_params
    )
    .map(argnames=["view", "data"], argvalues=total_patrol_dist_converted)
)


# %% [markdown]
# ## Merge per group Total Patrol Distance SV widgets

# %%
# parameters

patrol_dist_grouped_widget_params = dict()

# %%
# call the task


patrol_dist_grouped_widget = (
    merge_widget_views.set_task_instance_id("patrol_dist_grouped_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=total_patrol_dist_sv_widgets, **patrol_dist_grouped_widget_params)
    .call()
)


# %% [markdown]
# ## Calculate Average Speed Per Group

# %%
# parameters

avg_speed_params = dict()

# %%
# call the task


avg_speed = (
    dataframe_column_mean.set_task_instance_id("avg_speed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="speed_kmhr", **avg_speed_params)
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Convert Average Speed units

# %%
# parameters

average_speed_converted_params = dict()

# %%
# call the task


average_speed_converted = (
    with_unit.set_task_instance_id("average_speed_converted")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(original_unit="km/h", new_unit="km/h", **average_speed_converted_params)
    .mapvalues(argnames=["value"], argvalues=avg_speed)
)


# %% [markdown]
# ## Create Single Value Widgets for Avg Speed Per Group

# %%
# parameters

avg_speed_sv_widgets_params = dict()

# %%
# call the task


avg_speed_sv_widgets = (
    create_single_value_widget_single_view.set_task_instance_id("avg_speed_sv_widgets")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Average Speed", decimal_places=1, **avg_speed_sv_widgets_params)
    .map(argnames=["view", "data"], argvalues=average_speed_converted)
)


# %% [markdown]
# ## Merge per group Avg Speed SV widgets

# %%
# parameters

avg_speed_grouped_widget_params = dict()

# %%
# call the task


avg_speed_grouped_widget = (
    merge_widget_views.set_task_instance_id("avg_speed_grouped_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=avg_speed_sv_widgets, **avg_speed_grouped_widget_params)
    .call()
)


# %% [markdown]
# ## Calculate Max Speed Per Group

# %%
# parameters

max_speed_params = dict()

# %%
# call the task


max_speed = (
    dataframe_column_max.set_task_instance_id("max_speed")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="speed_kmhr", **max_speed_params)
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Convert Max Speed units

# %%
# parameters

max_speed_converted_params = dict()

# %%
# call the task


max_speed_converted = (
    with_unit.set_task_instance_id("max_speed_converted")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(original_unit="km/h", new_unit="km/h", **max_speed_converted_params)
    .mapvalues(argnames=["value"], argvalues=max_speed)
)


# %% [markdown]
# ## Create Single Value Widgets for Max Speed Per Group

# %%
# parameters

max_speed_sv_widgets_params = dict()

# %%
# call the task


max_speed_sv_widgets = (
    create_single_value_widget_single_view.set_task_instance_id("max_speed_sv_widgets")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Max Speed", decimal_places=1, **max_speed_sv_widgets_params)
    .map(argnames=["view", "data"], argvalues=max_speed_converted)
)


# %% [markdown]
# ## Merge per group Max Speed SV widgets

# %%
# parameters

max_speed_grouped_widget_params = dict()

# %%
# call the task


max_speed_grouped_widget = (
    merge_widget_views.set_task_instance_id("max_speed_grouped_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=max_speed_sv_widgets, **max_speed_grouped_widget_params)
    .call()
)


# %% [markdown]
# ## Draw time series bar chart

# %%
# parameters

patrol_events_bar_chart_params = dict(
    time_interval=...,
)

# %%
# call the task


patrol_events_bar_chart = (
    draw_time_series_bar_chart.set_task_instance_id("patrol_events_bar_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        x_axis="time",
        y_axis="event_type_display",
        category="event_type_display",
        agg_function="count",
        color_column="event_type_colormap",
        plot_style={"xperiodalignment": "middle"},
        layout_style=None,
        widget_id=set_bar_chart_title,
        **patrol_events_bar_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_pe_groups)
)


# %% [markdown]
# ## Persist Patrols Bar Chart as Text

# %%
# parameters

patrol_events_bar_chart_html_url_params = dict(
    filename=...,
)

# %%
# call the task


patrol_events_bar_chart_html_url = (
    persist_text.set_task_instance_id("patrol_events_bar_chart_html_url")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="patrol_events_time_series_bar_chart",
        **patrol_events_bar_chart_html_url_params,
    )
    .mapvalues(argnames=["text"], argvalues=patrol_events_bar_chart)
)


# %% [markdown]
# ## Create Plot Widget for Patrol Events

# %%
# parameters

patrol_events_bar_chart_widget_params = dict()

# %%
# call the task


patrol_events_bar_chart_widget = (
    create_plot_widget_single_view.set_task_instance_id(
        "patrol_events_bar_chart_widget"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title=set_bar_chart_title, **patrol_events_bar_chart_widget_params)
    .map(argnames=["view", "data"], argvalues=patrol_events_bar_chart_html_url)
)


# %% [markdown]
# ## Merge Bar Plot Widget Views

# %%
# parameters

grouped_bar_plot_widget_merge_params = dict()

# %%
# call the task


grouped_bar_plot_widget_merge = (
    merge_widget_views.set_task_instance_id("grouped_bar_plot_widget_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        widgets=patrol_events_bar_chart_widget, **grouped_bar_plot_widget_merge_params
    )
    .call()
)


# %% [markdown]
# ## Draw Pie Chart for Patrols Events

# %%
# parameters

patrol_events_pie_chart_params = dict()

# %%
# call the task


patrol_events_pie_chart = (
    draw_pie_chart.set_task_instance_id("patrol_events_pie_chart")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        value_column="event_type_display",
        plot_style={"textinfo": "value"},
        label_column=None,
        color_column="event_type_colormap",
        layout_style=None,
        widget_id=set_pie_chart_title,
        **patrol_events_pie_chart_params,
    )
    .mapvalues(argnames=["dataframe"], argvalues=split_pe_groups)
)


# %% [markdown]
# ## Persist Patrols Pie Chart as Text

# %%
# parameters

pe_pie_chart_html_urls_params = dict(
    filename=...,
)

# %%
# call the task


pe_pie_chart_html_urls = (
    persist_text.set_task_instance_id("pe_pie_chart_html_urls")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="patrols_pie_chart",
        **pe_pie_chart_html_urls_params,
    )
    .mapvalues(argnames=["text"], argvalues=patrol_events_pie_chart)
)


# %% [markdown]
# ## Create Plot Widget for Patrol Events

# %%
# parameters

patrol_events_pie_chart_widgets_params = dict()

# %%
# call the task


patrol_events_pie_chart_widgets = (
    create_plot_widget_single_view.set_task_instance_id(
        "patrol_events_pie_chart_widgets"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title=set_pie_chart_title, **patrol_events_pie_chart_widgets_params)
    .map(argnames=["view", "data"], argvalues=pe_pie_chart_html_urls)
)


# %% [markdown]
# ## Merge Pie Chart Widget Views

# %%
# parameters

patrol_events_pie_widget_grouped_params = dict()

# %%
# call the task


patrol_events_pie_widget_grouped = (
    merge_widget_views.set_task_instance_id("patrol_events_pie_widget_grouped")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        widgets=patrol_events_pie_chart_widgets,
        **patrol_events_pie_widget_grouped_params,
    )
    .call()
)


# %% [markdown]
# ## Create meshgrid

# %%
# parameters

ltd_meshgrid_params = dict(
    auto_scale_or_custom_cell_size=...,
    crs=...,
)

# %%
# call the task


ltd_meshgrid = (
    create_meshgrid.set_task_instance_id("ltd_meshgrid")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        aoi=patrol_traj_cols_to_string, intersecting_only=False, **ltd_meshgrid_params
    )
    .call()
)


# %% [markdown]
# ## Crate Linear Time Density from Trajectory

# %%
# parameters

ltd_params = dict()

# %%
# call the task


ltd = (
    calculate_linear_time_density.set_task_instance_id("ltd")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        meshgrid=ltd_meshgrid,
        percentiles=[50.0, 60.0, 70.0, 80.0, 90.0, 100.0],
        **ltd_params,
    )
    .mapvalues(argnames=["trajectory_gdf"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Drop nan percentiles for map display

# %%
# parameters

drop_nan_percentiles_params = dict()

# %%
# call the task


drop_nan_percentiles = (
    drop_nan_values_by_column.set_task_instance_id("drop_nan_percentiles")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="percentile", **drop_nan_percentiles_params)
    .mapvalues(argnames=["df"], argvalues=ltd)
)


# %% [markdown]
# ## Sort LTD by percentile

# %%
# parameters

sort_percentile_values_params = dict()

# %%
# call the task


sort_percentile_values = (
    sort_values.set_task_instance_id("sort_percentile_values")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column_name="percentile",
        ascending=True,
        na_position="last",
        **sort_percentile_values_params,
    )
    .mapvalues(argnames=["df"], argvalues=drop_nan_percentiles)
)


# %% [markdown]
# ## Cast Percentile Column

# %%
# parameters

percentile_col_to_string_params = dict()

# %%
# call the task


percentile_col_to_string = (
    convert_column_values_to_string.set_task_instance_id("percentile_col_to_string")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(columns=["percentile"], **percentile_col_to_string_params)
    .mapvalues(argnames=["df"], argvalues=sort_percentile_values)
)


# %% [markdown]
# ## Time Density Colormap

# %%
# parameters

td_colormap_params = dict()

# %%
# call the task


td_colormap = (
    apply_color_map.set_task_instance_id("td_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        input_column_name="percentile",
        colormap="RdYlGn",
        output_column_name="percentile_colormap",
        **td_colormap_params,
    )
    .mapvalues(argnames=["df"], argvalues=percentile_col_to_string)
)


# %% [markdown]
# ## Rename patrol traj columns for map tooltip display

# %%
# parameters

patrol_td_rename_columns_params = dict()

# %%
# call the task


patrol_td_rename_columns = (
    map_columns.set_task_instance_id("patrol_td_rename_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        raise_if_not_found=True,
        drop_columns=[],
        retain_columns=["geometry", "percentile", "percentile_colormap"],
        rename_columns={"percentile": "Percentile"},
        **patrol_td_rename_columns_params,
    )
    .mapvalues(argnames=["df"], argvalues=td_colormap)
)


# %% [markdown]
# ## Create map layer from Time Density

# %%
# parameters

td_map_layer_params = dict()

# %%
# call the task


td_map_layer = (
    create_geojson_layer.set_task_instance_id("td_map_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
            all_geometry_are_none,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_fill_color": "percentile_colormap",
            "get_line_color": "percentile_colormap",
            "opacity": 0.45,
            "get_line_width": 1.55,
            "stroked": True,
            "filled": True,
            "get_elevation": 0,
            "get_point_radius": 1,
            "line_width_units": "pixels",
            "line_width_scale": 1,
            "line_width_min_pixels": 1,
            "line_width_max_pixels": 5,
            "extruded": False,
            "wireframe": False,
        },
        legend={
            "title": "Time Spent",
            "label_column": "Percentile",
            "color_column": "percentile_colormap",
        },
        **td_map_layer_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=patrol_td_rename_columns)
)


# %% [markdown]
# ## Combine styled layers with ltd

# %%
# parameters

merged_time_density_layers_params = dict()

# %%
# call the task


merged_time_density_layers = (
    combine_deckgl_map_layers.set_task_instance_id("merged_time_density_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        static_layers=[
            custom_amboseli_layer,
            custom_hotspot_layer,
            custom_protected_layer,
            create_hotspot_text_layer,
        ],
        **merged_time_density_layers_params,
    )
    .mapvalues(argnames=["grouped_layers"], argvalues=td_map_layer)
)


# %% [markdown]
# ## zoom to envelope ltd

# %%
# parameters

envelope_ltd_params = dict()

# %%
# call the task


envelope_ltd = (
    envelope_gdf.set_task_instance_id("envelope_ltd")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(expansion_factor=1.05, **envelope_ltd_params)
    .mapvalues(argnames=["gdf"], argvalues=patrol_td_rename_columns)
)


# %% [markdown]
# ## Get image gdf ltd zoom extent

# %%
# parameters

gdf_ltd_image_extent_params = dict()

# %%
# call the task


gdf_ltd_image_extent = (
    view_state_deck_gdf.set_task_instance_id("gdf_ltd_image_extent")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(pitch=0, bearing=0, **gdf_ltd_image_extent_params)
    .mapvalues(argnames=["gdf"], argvalues=patrol_td_rename_columns)
)


# %% [markdown]
# ## Zoom to gdf extent

# %%
# parameters

zoom_ltd_gdf_extent_params = dict()

# %%
# call the task


zoom_ltd_gdf_extent = (
    custom_view_state_from_gdf.set_task_instance_id("zoom_ltd_gdf_extent")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(max_zoom=20, **zoom_ltd_gdf_extent_params)
    .mapvalues(argnames=["gdf"], argvalues=envelope_ltd)
)


# %% [markdown]
# ## Combine ltd layers with view state

# %%
# parameters

zip_ltd_with_viewstate_params = dict()

# %%
# call the task


zip_ltd_with_viewstate = (
    zip_groupbykey.set_task_instance_id("zip_ltd_with_viewstate")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merged_time_density_layers, zoom_ltd_gdf_extent],
        **zip_ltd_with_viewstate_params,
    )
    .call()
)


# %% [markdown]
# ## Draw linear time density map

# %%
# parameters

td_ecomap_params = dict()

# %%
# call the task


td_ecomap = (
    draw_map.set_task_instance_id("td_ecomap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=base_map_defs,
        legend_style={"placement": "bottom-right"},
        static=False,
        title=None,
        max_zoom=10,
        widget_id=set_ltd_map_title,
        **td_ecomap_params,
    )
    .mapvalues(argnames=["geo_layers", "view_state"], argvalues=zip_ltd_with_viewstate)
)


# %% [markdown]
# ## Persist Ecomap as Text

# %%
# parameters

td_ecomap_html_url_params = dict(
    filename=...,
)

# %%
# call the task


td_ecomap_html_url = (
    persist_text.set_task_instance_id("td_ecomap_html_url")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename_suffix="time_density",
        **td_ecomap_html_url_params,
    )
    .mapvalues(argnames=["text"], argvalues=td_ecomap)
)


# %% [markdown]
# ## Combine homerange ltd map path with zoom value

# %%
# parameters

zip_ltd_value_params = dict()

# %%
# call the task


zip_ltd_value = (
    zip_groupbykey.set_task_instance_id("zip_ltd_value")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[gdf_ltd_image_extent, td_ecomap_html_url], **zip_ltd_value_params
    )
    .call()
)


# %% [markdown]
# ## Convert homerange ltd html to png

# %%
# parameters

generate_ltd_png_params = dict()

# %%
# call the task


generate_ltd_png = (
    adjust_map_zoom_and_screenshot.set_task_instance_id("generate_ltd_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        screenshot_config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **generate_ltd_png_params,
    )
    .mapvalues(argnames=["view_state", "input_file"], argvalues=zip_ltd_value)
)


# %% [markdown]
# ## Create time density map widget

# %%
# parameters

td_map_widget_params = dict()

# %%
# call the task


td_map_widget = (
    create_map_widget_single_view.set_task_instance_id("td_map_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(title="Home Range Metrics", **td_map_widget_params)
    .map(argnames=["view", "data"], argvalues=td_ecomap_html_url)
)


# %% [markdown]
# ## Merge Time Density Map Widget Views

# %%
# parameters

td_grouped_map_widget_params = dict()

# %%
# call the task


td_grouped_map_widget = (
    merge_widget_views.set_task_instance_id("td_grouped_map_widget")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(widgets=td_map_widget, **td_grouped_map_widget_params)
    .call()
)


# %% [markdown]
# ## Summarize patrol efforts by ranger

# %%
# parameters

summarize_ranger_patrol_params = dict()

# %%
# call the task


summarize_ranger_patrol = (
    summarize_df.set_task_instance_id("summarize_ranger_patrol")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["patrol_subject"],
        reset_index=True,
        summary_params=[
            {
                "display_name": "no_of_patrols",
                "aggregator": "nunique",
                "column": "extra__patrol_id",
            },
            {
                "display_name": "total_distance",
                "aggregator": "sum",
                "column": "dist_meters",
                "original_unit": "m",
                "new_unit": "km",
            },
            {
                "display_name": "total_time",
                "aggregator": "sum",
                "column": "timespan_seconds",
                "original_unit": "s",
                "new_unit": "h",
            },
        ],
        **summarize_ranger_patrol_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Summarize events by guardian

# %%
# parameters

summarize_guardian_events_params = dict()

# %%
# call the task


summarize_guardian_events = (
    summarize_df.set_task_instance_id("summarize_guardian_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["patrol_subject"],
        reset_index=True,
        summary_params=[
            {"display_name": "no_of_events", "aggregator": "nunique", "column": "id"}
        ],
        **summarize_guardian_events_params,
    )
    .mapvalues(argnames=["df"], argvalues=pe_rename_display_columns)
)


# %% [markdown]
<<<<<<< HEAD
# ## Persist guardian patrol efforts csv
=======
# ## Persist a csv
>>>>>>> main

# %%
# parameters

<<<<<<< HEAD
persist_guardian_patrol_params = dict()
=======
persist_a_params = dict()
>>>>>>> main

# %%
# call the task


<<<<<<< HEAD
persist_guardian_patrol = (
    persist_df.set_task_instance_id("persist_guardian_patrol")
=======
persist_a = (
    persist_df.set_task_instance_id("persist_a")
>>>>>>> main
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
        filename=None,
<<<<<<< HEAD
        **persist_guardian_patrol_params,
=======
        **persist_a_params,
>>>>>>> main
    )
    .mapvalues(argnames=["df"], argvalues=summarize_ranger_patrol)
)


# %% [markdown]
<<<<<<< HEAD
# ## Persist guardian events csv
=======
# ## Persist b csv
>>>>>>> main

# %%
# parameters

<<<<<<< HEAD
persist_guardian_events_params = dict()
=======
persist_b_params = dict()
>>>>>>> main

# %%
# call the task


<<<<<<< HEAD
persist_guardian_events = (
    persist_df.set_task_instance_id("persist_guardian_events")
=======
persist_b = (
    persist_df.set_task_instance_id("persist_b")
>>>>>>> main
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
        filename=None,
<<<<<<< HEAD
        **persist_guardian_events_params,
=======
        **persist_b_params,
>>>>>>> main
    )
    .mapvalues(argnames=["df"], argvalues=summarize_guardian_events)
)


# %% [markdown]
# ## Summarize patrol by type

# %%
# parameters

summarized_patrol_types_params = dict()

# %%
# call the task


summarized_patrol_types = (
    summarize_df.set_task_instance_id("summarized_patrol_types")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["patrol_type"],
        reset_index=True,
        summary_params=[
            {
                "display_name": "no_of_patrols",
                "aggregator": "nunique",
                "column": "extra__patrol_id",
            },
            {
                "display_name": "total_distance",
                "aggregator": "sum",
                "column": "dist_meters",
                "original_unit": "m",
                "new_unit": "km",
            },
            {
                "display_name": "total_time",
                "aggregator": "sum",
                "column": "timespan_seconds",
                "original_unit": "s",
                "new_unit": "h",
            },
        ],
        **summarized_patrol_types_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Event types grouped by events total

# %%
# parameters

summarized_event_types_params = dict()

# %%
# call the task


summarized_event_types = (
    summarize_df.set_task_instance_id("summarized_event_types")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["event_type_display"],
        reset_index=True,
        summary_params=[
            {"display_name": "no_of_events", "aggregator": "nunique", "column": "id"}
        ],
        **summarized_event_types_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_pe_groups)
)


# %% [markdown]
# ## Persist event types

# %%
# parameters

persist_event_tefforts_params = dict()

# %%
# call the task


persist_event_tefforts = (
    persist_df.set_task_instance_id("persist_event_tefforts")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
        filename=None,
        **persist_event_tefforts_params,
    )
    .mapvalues(argnames=["df"], argvalues=summarized_event_types)
)


# %% [markdown]
# ## Add month name on trajs

# %%
# parameters

add_month_name_params = dict()

# %%
# call the task


add_month_name = (
    extract_date_parts.set_task_instance_id("add_month_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        date_column="extra__patrol_start_time",
        parts=["month", "day", "month_name"],
        **add_month_name_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_groups)
)


# %% [markdown]
# ## Summarize patrol efforts by month name

# %%
# parameters

summarize_month_patrol_params = dict()

# %%
# call the task


summarize_month_patrol = (
    summarize_df.set_task_instance_id("summarize_month_patrol")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["month_name"],
        reset_index=True,
        summary_params=[
            {
                "display_name": "no_of_patrols",
                "aggregator": "nunique",
                "column": "extra__patrol_id",
            },
            {
                "display_name": "total_distance",
                "aggregator": "sum",
                "column": "dist_meters",
                "original_unit": "m",
                "new_unit": "km",
            },
            {
                "display_name": "total_time",
                "aggregator": "sum",
                "column": "timespan_seconds",
                "original_unit": "s",
                "new_unit": "h",
            },
        ],
        **summarize_month_patrol_params,
    )
    .mapvalues(argnames=["df"], argvalues=add_month_name)
)


# %% [markdown]
# ## Persist month  patrol efforts

# %%
# parameters

persist_month_patrol_efforts_params = dict()

# %%
# call the task


persist_month_patrol_efforts = (
    persist_df.set_task_instance_id("persist_month_patrol_efforts")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
        filename=None,
        **persist_month_patrol_efforts_params,
    )
    .mapvalues(argnames=["df"], argvalues=summarize_month_patrol)
)


# %% [markdown]
# ## Summarize events by guardian

# %%
# parameters

summarize_events_params = dict()

# %%
# call the task


summarize_events = (
    summarize_df.set_task_instance_id("summarize_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["patrol_subject", "event_type_display"],
        reset_index=True,
        summary_params=[
            {"display_name": "no_of_events", "aggregator": "nunique", "column": "id"}
        ],
        **summarize_events_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_pe_groups)
)


# %% [markdown]
# ## Pivot guardian events

# %%
# parameters

pivot_guardian_events_params = dict()

# %%
# call the task


pivot_guardian_events = (
    pivot_df_1.set_task_instance_id("pivot_guardian_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        index_col="patrol_subject",
        columns_col="event_type_display",
        values_col="no_of_events",
        reset_idx=True,
        **pivot_guardian_events_params,
    )
    .mapvalues(argnames=["df"], argvalues=summarize_events)
)


# %% [markdown]
# ## Persist guardian patrol efforts pivot

# %%
# parameters

persist_pivot_patrol_efforts_params = dict()

# %%
# call the task


persist_pivot_patrol_efforts = (
    persist_df.set_task_instance_id("persist_pivot_patrol_efforts")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
<<<<<<< HEAD
        filename=None,
        **persist_pivot_patrol_efforts_params,
    )
    .mapvalues(argnames=["df"], argvalues=pivot_guardian_events)
)


# %% [markdown]
# ## Persist guardian patrol efforts

# %%
# parameters

persist_gua_patrol_efforts_params = dict()

# %%
# call the task


persist_gua_patrol_efforts = (
    persist_df.set_task_instance_id("persist_gua_patrol_efforts")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
        filename=None,
        **persist_gua_patrol_efforts_params,
    )
    .mapvalues(argnames=["df"], argvalues=summarize_events)
)


# %% [markdown]
# ## Convert patrol events pie chart html to png

# %%
# parameters

patrol_pie_chart_png_params = dict()

# %%
# call the task


patrol_pie_chart_png = (
    html_to_png.set_task_instance_id("patrol_pie_chart_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **patrol_pie_chart_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=pe_pie_chart_html_urls)
)


# %% [markdown]
# ## Convert patrol events time series bar chart html to png

# %%
# parameters

patrol_bar_chart_png_params = dict()

# %%
# call the task


patrol_bar_chart_png = (
    html_to_png.set_task_instance_id("patrol_bar_chart_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 5,
        },
        **patrol_bar_chart_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=patrol_events_bar_chart_html_url)
)


# %% [markdown]
# ## Create context cover page

# %%
# parameters

context_cover_page_params = dict()

# %%
# call the task


context_cover_page = (
    create_guardians_ctx_cover.set_task_instance_id("context_cover_page")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        report_period=time_range, prepared_by="Ecoscope", **context_cover_page_params
    )
    .call()
)


# %% [markdown]
# ## Persist context page for guardians report

# %%
# parameters

persist_ctx_page_params = dict()

# %%
# call the task


persist_ctx_page = (
    create_context_page_lg.set_task_instance_id("persist_ctx_page")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        template_path=persist_cover_page,
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        context=context_cover_page,
        filename="context_page.docx",
        **persist_ctx_page_params,
    )
    .call()
)


# %% [markdown]
# ## Group context values together

# %%
# parameters

group_context_values_params = dict()

# %%
# call the task


group_context_values = (
    zip_groupbykey.set_task_instance_id("group_context_values")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[
            generate_events_png,
            generate_trajs_png,
            generate_ltd_png,
            patrol_pie_chart_png,
            patrol_bar_chart_png,
            persist_month_patrol_efforts,
            persist_pivot_patrol_efforts,
            persist_guardian_events,
            persist_guardian_patrol,
            persist_event_tefforts,
            split_patrol_traj_groups,
        ],
        **group_context_values_params,
    )
    .call()
)


# %% [markdown]
# ## Create individual patrol context page

# %%
# parameters

individual_patrol_ctx_page_params = dict()

# %%
# call the task


individual_patrol_ctx_page = (
    guardians_ctx.set_task_instance_id("individual_patrol_ctx_page")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(grouper_name=groupers, **individual_patrol_ctx_page_params)
    .mapvalues(
        argnames=[
            "events_map",
            "patrols_trajectories_map",
            "time_density_map",
            "pie_chart",
            "time_series_bar_chart",
            "monthly_csv",
            "patrol_subject_pivot_csv",
            "patrol_subject_events_csv",
            "patrol_subject_stats_csv",
            "events_recorded_csv",
            "df",
        ],
        argvalues=group_context_values,
    )
)


# %% [markdown]
# ## Create grouper word doc

# %%
# parameters

create_grouper_doc_params = dict()

# %%
# call the task


create_grouper_doc = (
    generate_guardians_report.set_task_instance_id("create_grouper_doc")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        template_path=persist_indv_subject_page,
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        box_h_inches=6.48,
        box_w_inches=3.85,
        validate_images=True,
        **create_grouper_doc_params,
    )
    .mapvalues(argnames=["context"], argvalues=individual_patrol_ctx_page)
=======
        filename=None,
        **persist_pivot_patrol_efforts_params,
    )
    .mapvalues(argnames=["df"], argvalues=pivot_guardian_events)
>>>>>>> main
)


# %% [markdown]
# ## Persist guardian patrol efforts

# %%
# parameters

persist_gua_patrol_efforts_params = dict()

# %%
# call the task


persist_gua_patrol_efforts = (
    persist_df.set_task_instance_id("persist_gua_patrol_efforts")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filetype="csv",
        filename=None,
        **persist_gua_patrol_efforts_params,
    )
    .mapvalues(argnames=["df"], argvalues=summarize_events)
)


# %% [markdown]
# ## Convert patrol events pie chart html to png

# %%
# parameters

patrol_pie_chart_png_params = dict()

# %%
# call the task


patrol_pie_chart_png = (
    html_to_png.set_task_instance_id("patrol_pie_chart_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 1,
        },
        **patrol_pie_chart_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=pe_pie_chart_html_urls)
)


# %% [markdown]
# ## Convert patrol events time series bar chart html to png

# %%
# parameters

patrol_bar_chart_png_params = dict()

# %%
# call the task


patrol_bar_chart_png = (
    html_to_png.set_task_instance_id("patrol_bar_chart_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 10,
            "max_concurrent_pages": 5,
        },
        **patrol_bar_chart_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=patrol_events_bar_chart_html_url)
)


# %% [markdown]
# ## Create Dashboard with Patrol Map Widgets

# %%
# parameters

patrol_dashboard_params = dict(
    warning=...,
)

# %%
# call the task


patrol_dashboard = (
    gather_dashboard.set_task_instance_id("patrol_dashboard")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        details=workflow_details,
<<<<<<< HEAD
        widgets=[
            events_grouped_map_widget,
            trajs_grouped_map_widget,
            total_patrols_grouped_sv_widget,
            patrol_time_grouped_widget,
            patrol_dist_grouped_widget,
            avg_speed_grouped_widget,
            max_speed_grouped_widget,
            grouped_bar_plot_widget_merge,
            patrol_events_pie_widget_grouped,
            td_grouped_map_widget,
        ],
=======
        widgets=[],
>>>>>>> main
        groupers=groupers,
        time_range=time_range,
        **patrol_dashboard_params,
    )
    .call()
)
